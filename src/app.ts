/**
 * Shell de la app. Por ahora monta un placeholder; el router y el layout
 * navegable se implementan en A18 (Grupo A) y se enriquecen en el Grupo C.
 */
export function mountApp(root: HTMLElement): void {
  root.textContent = '练中文';
}
